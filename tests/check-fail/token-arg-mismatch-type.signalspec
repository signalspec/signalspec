//# incorrect argument on `val`
@test(#up, (9))
with Seq(1..10, _) def main() = {
    val((9, 9))
//      ^^^^^^ expected `1..10`, found `(9, 9)`
}

//# incorrect argument on `val`
@test(#up, (9))
with Seq(1..10, _) def main() = {
    val(20)
//      ^^ expected `1..10`, found `20`
}
