@test(#up, ())
with Seq(type _, _) def main() = seq {
//# `on` block used in a context without an upwards signal
    on foo() {}
//  ^^^^^^^^^^^ requires a signal to act on
}

@test(#up, ())
with Seq(type _, _) def main() = seq {
//# `on` block used in a context without an upwards signal
    on foo {}
//  ^^^^^^^^^ requires a signal to act on
}
