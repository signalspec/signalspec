//# mode mismatch on child protocol `foo`
protocol Foo() #dn {
    foo: Seq(type #x, #up)
//  ^^^^^^^^^^^^^^^^^^^^^^ mode of this protocol is `#up` but the parent has `#dn`
}

@test(#up, ())
with Seq(type _, _) def main() = new Foo() {}
