@test(#up, ())
with Seq(#x, _) def main() = {
//# invalid expression for direction
    repeat #foo 6 {
//         ^^^^ expected constant `#up` or `#dn`, found #foo

    }

//# invalid expression for direction
//# undefined variable `dn`
    alt (dn, 1) #x {
//       ^^ not found
//      ^^^^^^^ expected constant `#up` or `#dn`, found (<error>, 1) 
    }

// if expression is itself an error, don't report a new error
//# undefined variable `xyz`
    alt xyz (1, 2) {
//      ^^^ not found
    }
}
