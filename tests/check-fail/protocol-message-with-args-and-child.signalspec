protocol Foo() #dn {
//# protocol message with child protocol cannot have arguments
    foo(var(#dn) #a): Seq(type #a, #dn)
//  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ has arguments and child protocol
}

@test(#dn, ())
with Seq(type _, _) def main() = new Foo() {}
