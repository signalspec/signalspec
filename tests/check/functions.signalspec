let bit = #0 | #1

with Seq([bit, bit, bit])
def test_signed(): Seq(#-4..#3) {
        on x { (signed(#3, x)) }
        on x { (signed(#3, x)) }
        on x { (signed(#3, x)) }
}

test @both(Seq([bit, bit, bit])) { [#1, #0, #1]; [#0, #0, #1]; [#0, #0, #0] } test_signed() { #-3; #1; #0 };
