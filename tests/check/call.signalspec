with Seq(#a|#b)
def main(a, b) {
    a
    other(a, b)
    b
}

with Seq(#a|#b)
def other(x, y) {
    x
    y
}

test @both(Seq(#a|#b)){ #a; #a; #b; #b } main(#a, #b);
test fail @up(Seq(#a|#b)){ #a; #a } main(#a, #b);
