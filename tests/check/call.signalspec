with Seq(#a|#b)
def main(a, b) {
    val(a)
    other(a, b)
    val(b)
}

with Seq(#a|#b)
def other(x, y) {
    val(x)
    val(y)
}

test @both(Seq(#a|#b)){ val(#a); val(#a); val(#b); val(#b) } main(#a, #b);
test fail @up(Seq(#a|#b)){ val(#a); val(#a) } main(#a, #b);
