with Seq(#h|#l)
def main(): Seq(#0..#3) {
  repeat {
    on val(v) {
      val(#h)
      repeat v { val(#h) }
    }
    repeat #1..#10 { val(#l) }
  }
}

test @up(Seq(#h|#l)) {
  val(#h);
  val(#l);
  val(#h);
  val(#h);
  val(#l);
  val(#h);
  val(#h);
  val(#h);
  val(#l)
} main() { val(#0); val(#1); val(#2) };
