let x = #x

with Seq(#x|#y|#z)
def main(a) = {
    let y = a
    let z = #z
    val(x)
    val(y)
    val(z)
}

test @both(Seq(#x|#y|#z)) { val(#x); val(#y); val(#z) } | main(#y);
test fail @up(Seq(#x|#y|#z)) { val(#x); val(#y); val(#z) } | main(#z);
