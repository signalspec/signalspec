let x = #x

with Seq(#x|#y|#z)
def main(a) {
    let y = a
    let z = #z
    x
    y
    z
}

test @both(Seq(#x|#y|#z)) { #x; #y; #z } main(#y);
test fail @up(Seq(#x|#y|#z)) { #x; #y; #z } main(#z);
