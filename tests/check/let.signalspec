let x = #x

with Seq(#x|#y|#z, dir)
def main(var(dir) a) = {
    let y = a
    let z = #z
    val(x)
    val(y)
    val(z)
}

test @both(Seq(#x|#y|#z, _)) { val(#x); val(#y); val(#z) } | main(#y);
test fail @up(Seq(#x|#y|#z, _)) { val(#x); val(#y); val(#z) } | main(#z);
