@test(#both, (#a, #b, #b))
with Seq(type #a|#b, _) def main() = seq {
    inner() | seq {
        val(#x)
        val(#y)
        val(#y)
    }
}

with Seq(type #a|#b, dir) def inner() = new Seq(type #x|#y, dir) {
    repeat {
        on val(i) {
            val(i[#x=#a, #y=#b])
        }
    }
}
