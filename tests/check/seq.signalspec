
with Seq(#a|#b|#c)
def main() = {
    val(#a)
    val(#b)
    val(#c)
}

test @both(Seq(#a|#b|#c)) { val(#a); val(#b); val(#c) } | main();
test fail @up(Seq(#a|#b|#c)) { val(#a); val(#b); val(#a) } | main();
test fail @up(Seq(#a|#b|#c)) { val(#c); val(#c); val(#c) } | main();
